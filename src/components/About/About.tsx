import { Container } from "react-bootstrap";

function About() {
    return (
        <Container fluid className="about-section">
            About
        </Container>
    );
}

export default About;
